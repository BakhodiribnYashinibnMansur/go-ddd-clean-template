package schema

// Table name
const TableRelation = "relation"

// Relation table columns
const (
	RelationType      = "type"
	RelationID        = "id"
	RelationName      = "name"
	RelationCreatedAt = "created_at"
)
