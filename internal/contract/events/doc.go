// Package events is the Published Language (Evans): versioned, immutable
// event contracts published on the event bus and consumed by subscribers
// in other bounded contexts. Internal domain events of a producing BC are
// mapped into these contracts at publish time.
package events
